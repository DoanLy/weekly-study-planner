# Weekly Study Planner

Personal weekly study planner built with React and Vite.

## Development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

The app stores a local browser copy of the study data and synchronizes it with
the configured Google Apps Script endpoint. The selected week can be pinned
with the checkbox below the week label so that a reload opens the same week.

## Deployment

The `main` branch is connected to Vercel. Production builds are published at
[files-mentioned-by-the-user-weeklyp.vercel.app](https://files-mentioned-by-the-user-weeklyp.vercel.app/).
